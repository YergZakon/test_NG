# Railway: для этого сервиса задайте Root Directory = webapp
[build]
builder = "nixpacks"

[deploy]
startCommand = "python seed.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
